# Legacy Custom Commands

These are the old format custom commands from before Claude Code merged slash commands and skills. They were originally placed in `.claude/commands/` as individual markdown files.

Claude Code now uses the newer skills format at `.claude/skills/[skill-name]/SKILL.md`. The commands in this folder are kept for reference only.
